{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Api": {
    "BaseUrl": "http://127.0.0.1:5080"
  },
  "DevTokens": {
    "_comment": "Local dev only. Generate with: dotnet run --project src/Kinsmen.Api --no-build -- --demo-token <subject> <role>  (see docs/backend/GETTING-STARTED.md). Tokens expire after 30 minutes — never commit real values here. ActiveRole picks which one gets sent; switch it when testing barber screens vs customer screens.",
    "ActiveRole": "Customer",
    "Customer": "",
    "Barber": "",
    "Admin": ""
  }
}
